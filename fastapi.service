[Unit]
Description=FastAPI Application Service
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/FinalSO_2
ExecStart=/home/ubuntu/FinalSO_2/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
